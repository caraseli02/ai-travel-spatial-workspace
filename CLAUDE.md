# Wayfarer Agent Entry

The canonical agent entry file is [`AGENTS.md`](AGENTS.md). Read it for the project overview, quick-start commands, work rules, hard constraints, the Fresh Session Map, and topic-doc routing.

This pointer exists so Claude/Cursor tooling that looks for `CLAUDE.md` lands on the same single source of truth. Do not duplicate `AGENTS.md` content here — keeping two copies causes them to drift.
